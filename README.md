# Crawling papers easier

Crawling research articles with **Selenium**, **BeautifulSoup** in Python.

Available websites
- Pubmed
- KMbase
- KISS

# How to use
1. Select a crawling site (among the available lists above).
2. Choose keywords.
3. Run `{}_crawling` function.
